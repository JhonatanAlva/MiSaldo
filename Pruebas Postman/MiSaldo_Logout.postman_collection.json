{
  "info": {
    "name": "MiSaldo - Logout",
    "_postman_id": "logout-collection-001",
    "description": "Request para cerrar sesión en MiSaldo (/auth/logout). Asegúrate de hacer login primero para tener la cookie/token.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Cerrar sesión (Logout)",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          },
          {
            "key": "Cookie",
            "value": "token={{token}}"
          }
        ],
        "url": {
          "raw": "http://localhost:5000/auth/logout",
          "protocol": "http",
          "host": [
            "localhost"
          ],
          "port": "5000",
          "path": [
            "auth",
            "logout"
          ]
        }
      },
      "response": [],
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Estado 200 OK\", function () {",
              "    pm.response.to.have.status(200);",
              "});",
              "",
              "pm.test(\"Respuesta contiene mensaje de cierre de sesión\", function () {",
              "    var json = pm.response.json();",
              "    pm.expect(json).to.have.property('mensaje');",
              "    pm.expect(json.mensaje).to.be.oneOf(['Sesión cerrada', 'Sesión cerrada.']);",
              "});",
              "",
              "// Verificar que el servidor intente borrar la cookie 'token' (Set-Cookie contiene token=; o token=; Max-Age=0)",
              "pm.test(\"Set-Cookie borra token (si está presente)\", function () {",
              "    var setCookie = pm.response.headers.get('set-cookie') || pm.response.headers.get('Set-Cookie');",
              "    if (setCookie) {",
              "        pm.expect(setCookie.toLowerCase()).to.include('token=');",
              "    } else {",
              "        pm.skip('No se devolvió header Set-Cookie en la respuesta');",
              "    }",
              "});"
            ],
            "type": "text/javascript"
          }
        }
      ]
    }
  ]
}
